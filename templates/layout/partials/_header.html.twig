<header>
    <nav class="navbar navbar-expand-md navbar-light navbarTop">
        <div class="container">
            <a class="navbar-brand" href="{{ path('app_home') }}"><img src="{{ asset('images/logo.png')}}" alt="logo" width="50px" height="60px"></a>
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('app_home') }}">Home</a>
                    </li>
                    {% if is_granted('ROLE_ADMIN') %}
                        <li class="nav-item active">
                            <a class="nav-link" href="{{ path('app_trick_create') }}">Trick</a>
                        </li>
                    {% endif %}
    
                    {% if app.user %}
                        <li class="nav-item">
                            <a class="nav-link" href="#">Compte</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_logout') }}">Logout</a>
                        </li>
                    {% endif %}
                    
                    {% if not app.user %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_registration') }}">Sign In</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_login') }}">Sign Up</a>
                        </li>
                    {% endif %}
                </ul>
        </div>
    </nav>
</header>


<header class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom shadow-sm navbarBottom">
    <nav class="my-2 my-md-0 me-md-3">
        <a class="p-2" href="{{ path('app_home') }}"><i class="fa fa-home"></i></a>
        <a class="p-2" href="{{ path('app_trick_create') }}"><i class="fa fa-bookmark"></i></a>
        {% if app.user %}
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_admin') }}"><i class="fas fa-sign-in-alt"></i></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_logout') }}">Logout</a>
            </li>
        {% else %}
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_login') }}"><i class="fas fa-sign-in-alt"></i></a>
            </li>
        {% endif %}
    </nav>
</header>
