{{ form_start(form) }}
{{ form_row(form.name) }}
{{ form_row(form.description) }}
{{ form_row(form.category) }}
{{ form_errors(form.mainImage) }}
{% if form.mainImage.vars.value.id is defined %}
    <p>Update images: </p>
    <img src="{{ UploadDirectory~trick.mainImage.name}}" alt=""
         class="img img-responsive editImageArticle">
{% else %}
    <p>Add images: </p>
{% endif %}
{{ form_row(form.mainImage)}}
{#<br>#}
{#<p>Add videos (Youtube ou Dailymotion)</p>#}
{#<ul class="video" data-prototype="{{ form_widget(form.videos.vars.prototype)|e('html_attr') }}"></ul>#}
{{ form_rest(form) }}