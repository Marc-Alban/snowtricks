<header class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom shadow-sm navbarTop">
    <p class="h5 my-0 me-md-auto fw-normal"><a href="{{ path('home') }}">Snowtricks</a></p>
    <nav class="my-2 my-md-0 me-md-3">
        <a class="p-2 text-dark {% if current_menu is defined and current_menu == 'home' %}active{% endif %}" href="{{ path('home') }}">Home</a>
        <a class="p-2 text-dark {% if current_menu is defined and current_menu == 'tricks' %}active{% endif %}" href="{{ path('trick_index') }}">Tricks</a>
        <a class="p-2 text-dark {% if current_menu is defined and current_menu == 'signIn' %}active{% endif %}" href="{{ path('register') }}">Sign In</a>
        <a class="p-2 text-dark {% if current_menu is defined and current_menu == 'signUp' %}active{% endif %}" href="{{ path('login') }}">Sign Out</a>
    </nav>
</header>
